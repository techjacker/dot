#!/usr/bin/env bash

precommitinstall() {
  pre-commit install
  pre-commit install-hooks
}

alias pri=precommitinstall
alias prr="pre-commit run --all-files"
alias prrskip="SKIP='' pre-commit run --all-files"
alias pra="pre-commit autoupdate"
alias pru="pre-commit uninstall"
